#include <iostream>
#include <vector>

using namespace std;

#define nl '\n'

int main() {
    int n, m;
    cin >> n >> m;

    vector<vector<pair<int, char>>> graph(n, vector<pair<int, char>>());

    for (int i = 0; i < n; i++) {
        int s, d;
        char c;
        cin >> s >> d >> c;

        
    }
}
